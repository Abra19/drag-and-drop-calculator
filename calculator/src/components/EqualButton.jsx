import { Button } from 'react-bootstrap';

const EqualButton = () => (
  <div className="equality border-4">
    <Button className="equal base-text text-white" disabled>=</Button>
  </div>
);

export default EqualButton;
